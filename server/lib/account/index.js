const { getAccountEditDetails } = require('./account-edit-details')
const { saveAccountDetails } = require('./account-save-details')
const { savePasswordDetails } = require('./password-save-details')

module.exports = {
  getAccountEditDetails,
  saveAccountDetails,
  savePasswordDetails
}
